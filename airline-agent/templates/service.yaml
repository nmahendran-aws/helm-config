apiVersion: v1
kind: Service
metadata:
  name: {{ include "airline-agent.fullname" . }}
  labels:
    {{- include "airline-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "airline-agent.selectorLabels" . | nindent 4 }}
